import React from "react";

const HomePage = () => {
    return (
        <div className="container mt-5">
            <h1>Bem-vindo(a) ao HomePage!</h1>
            <p>Aqui está o painel inicial da aplicação.</p>
        </div>
    );
};

export default HomePage;